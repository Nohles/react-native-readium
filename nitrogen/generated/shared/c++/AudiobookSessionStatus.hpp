///
/// AudiobookSessionStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::readium {

  /**
   * An enum which can be represented as a JavaScript union (AudiobookSessionStatus).
   */
  enum class AudiobookSessionStatus {
    IDLE      SWIFT_NAME(idle) = 0,
    LOADING      SWIFT_NAME(loading) = 1,
    READY      SWIFT_NAME(ready) = 2,
    PLAYING      SWIFT_NAME(playing) = 3,
    PAUSED      SWIFT_NAME(paused) = 4,
    ENDED      SWIFT_NAME(ended) = 5,
    ERROR      SWIFT_NAME(error) = 6,
  } CLOSED_ENUM;

} // namespace margelo::nitro::readium

namespace margelo::nitro {

  // C++ AudiobookSessionStatus <> JS AudiobookSessionStatus (union)
  template <>
  struct JSIConverter<margelo::nitro::readium::AudiobookSessionStatus> final {
    static inline margelo::nitro::readium::AudiobookSessionStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::readium::AudiobookSessionStatus::IDLE;
        case hashString("loading"): return margelo::nitro::readium::AudiobookSessionStatus::LOADING;
        case hashString("ready"): return margelo::nitro::readium::AudiobookSessionStatus::READY;
        case hashString("playing"): return margelo::nitro::readium::AudiobookSessionStatus::PLAYING;
        case hashString("paused"): return margelo::nitro::readium::AudiobookSessionStatus::PAUSED;
        case hashString("ended"): return margelo::nitro::readium::AudiobookSessionStatus::ENDED;
        case hashString("error"): return margelo::nitro::readium::AudiobookSessionStatus::ERROR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum AudiobookSessionStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::readium::AudiobookSessionStatus arg) {
      switch (arg) {
        case margelo::nitro::readium::AudiobookSessionStatus::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::readium::AudiobookSessionStatus::LOADING: return JSIConverter<std::string>::toJSI(runtime, "loading");
        case margelo::nitro::readium::AudiobookSessionStatus::READY: return JSIConverter<std::string>::toJSI(runtime, "ready");
        case margelo::nitro::readium::AudiobookSessionStatus::PLAYING: return JSIConverter<std::string>::toJSI(runtime, "playing");
        case margelo::nitro::readium::AudiobookSessionStatus::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::readium::AudiobookSessionStatus::ENDED: return JSIConverter<std::string>::toJSI(runtime, "ended");
        case margelo::nitro::readium::AudiobookSessionStatus::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert AudiobookSessionStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("loading"):
        case hashString("ready"):
        case hashString("playing"):
        case hashString("paused"):
        case hashString("ended"):
        case hashString("error"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
